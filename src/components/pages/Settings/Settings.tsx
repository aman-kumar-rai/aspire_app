import styles from "./style.module.css"

const Settings = () => {
    return (
        <div className={styles.container}>
            <h1 className={styles.label}>Settings</h1>
        </div>
    )
}

export default Settings;