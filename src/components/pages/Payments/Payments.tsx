import styles from "./style.module.css"

const Payments = () => {
    return (
        <div className={styles.container}>
            <h1 className={styles.label}>Payments</h1>
        </div>
    )
}

export default Payments;